# GOIES — Python dependencies
# Fix #1: httpx replaces requests for async Ollama calls

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # file uploads

# Async HTTP (Fix #1 — replaces blocking requests)
httpx>=0.27.0

# Graph engine
networkx>=3.2

# LLM / NLP
# (Ollama runs locally — no Python client needed; httpx talks to its REST API)

# Embeddings
node2vec>=0.4.6
scikit-learn>=1.4.0
numpy>=1.26.0

# OSINT / ingestion
feedparser>=6.0.11           # RSS/Atom
pdfplumber>=0.11.0           # PDF extraction
python-docx>=1.1.0           # DOCX extraction
beautifulsoup4>=4.12.0       # URL scraping
lxml>=5.1.0

# Report generation
reportlab>=4.1.0             # PDF reports

# Utilities
python-dotenv>=1.0.1
aiofiles>=23.2.1

# Optional — keep requests for legacy ingestor URL fetch (sync path)
requests>=2.31.0
